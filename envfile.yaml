
name: my-env

# Specify the dependencies
dependencies:
  # The Python version
  - python=3.6.2
  # The numpy package
  - numpy
  # The scikit-learn package
  - scikit-learn
  # The pandas package
  - pandas
  # The azureml-defaults package
  - azureml-defaults

# Specify the channels
channels:
  - anaconda
  - conda-forge
  - microsoft-azure